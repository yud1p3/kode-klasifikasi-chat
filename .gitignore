target/
dist/
node_modules/
.env
*.zip
.env-key
.env.tmp
.codewhaleignore

.codewhale/

# Binary hasil build tool benchmark
meili-benchmark/meili-benchmark

# .env VPS (berisi secret produksi) — JANGAN di-commit
backend/.env.vps

# Backup file lama
*.old

# Kunci privat extension SRIKANDI — JANGAN di-commit (hanya kunci publik di manifest)
srikandi-extension/keys/
